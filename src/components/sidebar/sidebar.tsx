import {
  SidebarContainer,
  SidebarContent,
} from './styles';

export const Sidebar = ({}) => {
  return (
    <SidebarContainer>
      <SidebarContent>text</SidebarContent>
    </SidebarContainer>
  );
};
